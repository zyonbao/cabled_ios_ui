# slide6_console depends on the executor_ios package (same repo) plus PySide6.
# Install executor_ios deps first: pip install -r ../executor_ios/requirements.txt
PySide6>=6.6
# HEIC/HEIF decoding for the album (DCIM) thumbnails and large-image viewer.
# Used instead of Qt's optional heif image plugin so packaging is deterministic
# (pillow-heif bundles its own libheif).
pillow-heif>=1.0

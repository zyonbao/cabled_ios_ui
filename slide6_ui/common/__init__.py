"""Shared building blocks for slide6_ui sidebar tabs.

Holds cross-tab utilities (async worker, AFC browser widgets, sidebar tab
container, tunnel bootstrap). Callers import the submodules explicitly to keep
package import side effects minimal.
"""

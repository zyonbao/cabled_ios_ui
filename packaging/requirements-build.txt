# Build-time dependencies for packaging CablediOS.app with Nuitka.
# Install into the same environment that has the runtime deps:
#   .venv/bin/python -m pip install -r packaging/requirements-build.txt
#
# Runtime deps (installed separately) live in:
#   - executor_ios/requirements.txt  (requests, pymobiledevice3, ...)
#   - slide6_console/requirements.txt (PySide6)

nuitka>=2.4
ordered-set>=4.1.0
zstandard>=0.22.0
